-- Monitor configuration — edit to match your setup.
-- hl.monitor({ output = "DP-1", mode = "1920x1080@144", position = "0x0", scale = 1 })
-- Fallback rule: any monitor not explicitly configured gets preferred mode.
hl.monitor({ output = "", mode = "preferred", position = "auto", scale = 1 })
