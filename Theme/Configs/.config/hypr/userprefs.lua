-- User preferences — override defaults here; sourced last so settings take precedence.
-- Example: device-specific touchpad tuning
hl.device({
    name            = "synps/2-synaptics-touchpad",
    sensitivity     = -0.3,
    accel_profile   = "flat",
})
